Leerer Platzhalter. Docker Compose hängt diesen Ordner nach `/schichtklar` ein,
solange `SCHICHTKLAR_HOST_DIR` in der `.env` nicht gesetzt ist. So bleibt die
Compose-Datei gültig, auch ohne Schichtklar/Shiftplanner auf dem Host.
